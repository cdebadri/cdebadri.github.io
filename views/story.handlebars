<div>
  <h3 class="text headline">{{ gameStory.story.title }}</h3>
  <div class="text">{{ gameStory.story.body }}</div>
  <h3 class="text headline">{{ gameStory.mission.title }}</h3>
  <div class="text">{{ gameStory.mission.body }}</div>
</div>